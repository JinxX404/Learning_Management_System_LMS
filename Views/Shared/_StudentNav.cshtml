@{
    var currentAction = ViewContext.RouteData.Values["action"]?.ToString();
}

<nav class="navbar navbar-expand flex-grow-1 px-4" aria-label="Main navigation">
    <div class="navbar-nav gap-2">
        <a class="nav-link @(currentAction == "Dashboard" ? "active" : "") d-flex align-items-center gap-2" 
           asp-controller="Student" 
           asp-action="Dashboard"
           aria-current="@(currentAction == "Dashboard" ? "page" : "false")">
            <svg class="icon-md" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" viewBox="0 0 24 24" aria-hidden="true">
                <path d="m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"></path>
                <polyline points="9 22 9 12 15 12 15 22"></polyline>
            </svg>
            <span>Dashboard</span>
        </a>
        
        <a class="nav-link @(currentAction == "MyCourses" || currentAction == "CourseDetails" || currentAction == "CourseContent" ? "active" : "") d-flex align-items-center gap-2" 
           asp-controller="Student" 
           asp-action="MyCourses"
           aria-current="@(currentAction == "MyCourses" ? "page" : "false")">
            <svg class="icon-md" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" viewBox="0 0 24 24" aria-hidden="true">
                <path d="M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H20v20H6.5a2.5 2.5 0 0 1 0-5H20"></path>
            </svg>
            <span>Courses</span>
        </a>
        
        <a class="nav-link @(currentAction == "Announcements" ? "active" : "") d-flex align-items-center gap-2" 
           asp-controller="Student" 
           asp-action="Announcements"
           aria-current="@(currentAction == "Announcements" ? "page" : "false")">
            <svg class="icon-md" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" viewBox="0 0 24 24" aria-hidden="true">
                <path d="M8 2v4"></path>
                <path d="M16 2v4"></path>
                <rect height="18" rx="2" width="18" x="3" y="4"></rect>
                <path d="M3 10h18"></path>
            </svg>
            <span>Announcements</span>
        </a>
        
         @* <a class="nav-link @(currentAction == "Grades" ? "active" : "") d-flex align-items-center gap-2" 
           asp-controller="Student" 
           asp-action="Grades"
           aria-current="@(currentAction == "Grades" ? "page" : "false")">
            <svg class="icon-md" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" viewBox="0 0 24 24" aria-hidden="true">
                <path d="M14 2v4a2 2 0 0 0 2 2h4"></path>
                <path d="M18 13v-3.5A1.5 1.5 0 0 0 16.5 8H3"></path>
                <path d="m10 16 4-4"></path>
                <path d="m14 16-4-4"></path>
                <path d="M10 12v10"></path>
                <path d="M4 12v10"></path>
                <path d="M18 22V10.5"></path>
            </svg>
            <span>Grades</span>
        </a> *@
        
        <a class="nav-link @(currentAction == "Profile" ? "active" : "") d-flex align-items-center gap-2" 
           asp-controller="Student" 
           asp-action="Profile"
           aria-current="@(currentAction == "Profile" ? "page" : "false")">
            <svg class="icon-md" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" viewBox="0 0 24 24" aria-hidden="true">
                <path d="M19 21v-2a4 4 0 0 0-4-4H9a4 4 0 0 0-4 4v2"></path>
                <circle cx="12" cy="7" r="4"></circle>
            </svg>
            <span>Profile</span>
        </a>
        
        <a class="nav-link @(currentAction == "Assignments" || currentAction == "AssignmentsSubmissions" ? "active" : "") d-flex align-items-center gap-2" 
           asp-controller="Student" 
           asp-action="Assignments"
           aria-current="@(currentAction == "Assignments" ? "page" : "false")">
            <svg class="icon-md" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" viewBox="0 0 24 24" aria-hidden="true">
                <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path>
                <polyline points="14 2 14 8 20 8"></polyline>
                <line x1="16" y1="13" x2="8" y2="13"></line>
                <line x1="16" y1="17" x2="8" y2="17"></line>
                <polyline points="10 9 9 9 8 9"></polyline>
            </svg>
            <span>Assignments</span>
        </a>
    </div>
</nav>
